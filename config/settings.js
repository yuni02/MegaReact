const settings = {
    // DB연결에 필요한 정보를 설정 관련 파일에 객체로 관리
    connect : {
        host : 'localhost',
        port : '3306',
        user : 'root',
        password : '0000',
        database : 'mega'
    }
}

module.exports = settings;